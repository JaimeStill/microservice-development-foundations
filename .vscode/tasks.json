{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "overview-build",
            "command": "dotnet build",
            "type": "shell",
            "problemMatcher": "$msCompile",
            "options": {
                "cwd": "${workspaceFolder}/01-getting-started/overview/node"
            }
        }
    ]
}